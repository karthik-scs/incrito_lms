// Stub for "@zoom/download-manager" — not a published package; @zoom/meetingsdk only references
// it (via an AMD `define`) for an optional in-meeting file-download feature this app doesn't use.
// Aliased here (see next.config.ts) purely so the bundler has something to resolve.
export default {};
